# إعداد تذكيرات الجدول الدراسي داخل تطبيق نبض التفوق

تعمل صفحة `time-organizer.html` في المتصفح العادي دون أخطاء، وتحفظ المهام محليًا. وعند فتحها داخل تطبيق Capacitor، تستخدم طبقة الموقع إضافة `LocalNotifications` و`Preferences` إن كانت متاحة عبر `window.Capacitor.Plugins`.

> لا يعرّف جسر `window.NabdAndroid` الموثق حاليًا أي دوال للتذكيرات أو التخزين؛ فهو يوفّر `webReady` وواجهات PDF والفيديو والملفات والدردشة فقط. لذلك يجب أن يسجّل تطبيق Android إضافتي Capacitor القياسيتين كي تظهر الإشعارات الأصلية فعليًا.

## متطلبات تطبيق Android

ينبغي إبقاء الحزم التالية مثبّتة في مشروع التطبيق الأصلي على إصدار Capacitor 8 المتوافق:

```bash
npm install @capacitor/local-notifications@^8 @capacitor/preferences@^8
npx cap sync android
```

يجب أن يطلب التطبيق صلاحية الإشعارات في Android 13 وما بعده. يستدعي الموقع `checkPermissions()` ثم `requestPermissions()` قبل الجدولة. وعند الرفض يبقى الجدول يعمل وتظهر رسالة عربية للمستخدم، بدون حدوث خطأ في الصفحة.

## ما يفعله الموقع

| العملية | سلوك الويب | سلوك تطبيق Capacitor بعد تسجيل الإضافات |
|---|---|---|
| حفظ المهمة | `localStorage` | `Preferences` مع بديل `localStorage` |
| جدولة تذكير مستقبلي | يحفظ المهمة وينبّه أن الإشعار الأصلي غير متاح | `LocalNotifications.schedule()` باستخدام تاريخ ووقت الجهاز المحليين |
| تعديل الموعد | يحفظ القيمة الجديدة | يلغي التذكير السابق ثم ينشئ تذكيرًا جديدًا بنفس معرف المهمة |
| حذف/إكمال المهمة | يحذف أو يكمل المهمة محليًا | يلغي التذكير الأصلي المرتبط قبل تحديث المهمة |

## التحقق على جهاز Android

بعد تثبيت تطبيق Android المحدّث، افتح «الجدول الدراسي» وأضف مهمة بعد عدة دقائق مع تفعيل التذكير. امنح الصلاحية عند الطلب ثم أغلق التطبيق. ينبغي أن يظهر تذكير أصلي بعنوان «📚 تذكير دراسي» في الموعد المحدد. اختبر كذلك تعديل الموعد وحذف المهمة وإكمالها للتأكد من عدم بقاء تذكير قديم.

لا يتضمن هذا المستودع ملفات Android أو `capacitor.config.*` أو `package.json` الخاصة بالتطبيق الأصلي، لذلك لا يمكن تنفيذ `cap sync` أو اختبار الإشعار في الخلفية من هذا المستودع وحده. هذه الخطوات مطلوبة في مشروع تطبيق Android المالك للحزم المذكورة.
